apiVersion: v1
kind: Service
metadata:
  name: dashboard
  namespace: fraud-detection
  labels:
    app: dashboard
    component: frontend
spec:
  type: ClusterIP
  selector:
    app: dashboard
  ports:
    - port: 80
      targetPort: 3000
      protocol: TCP
      name: http
