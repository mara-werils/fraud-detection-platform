apiVersion: v1
kind: Service
metadata:
  name: scoring-service
  namespace: fraud-detection
  labels:
    app: scoring-service
    component: scoring
spec:
  type: ClusterIP
  selector:
    app: scoring-service
  ports:
    - port: 80
      targetPort: 8000
      protocol: TCP
      name: http
